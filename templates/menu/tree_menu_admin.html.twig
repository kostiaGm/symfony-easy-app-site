{% set page = request.query.get('page') is not empty ? request.query.get('page') : 1 %}

{% set lvl = 0  %}
{% for i,item in pagination %}
    {% if lvl == item.lvl %}
        </li>
    {% elseif item.lvl > lvl%}
        <ul>
    {% else %}
        </li>
        {% for i in 1..(lvl-item.lvl) %}
            </ul>
            </li>
        {% endfor %}
    {% endif %}
        <li class="list-group-item"> <a href="{{ path('menu_admin_edit', {id: item.id, page: page}) }}">{{ item.name }}</a>
        {% set lvl = item.lvl %}

{% endfor %}

{% for i in 1..lvl %}
        </li>
</ul>


{% endfor%}

<div class="navigation">
    {{ knp_pagination_render(pagination) }}
</div>