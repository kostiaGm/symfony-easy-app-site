{% set route =  user.id is null ? 'app_user_new' : 'app_user_edit' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
     {% include 'elements/action_buttons_form.html.twig' with {back_to_list_path: path('app_user_index')}%}
{{ form_end(form) }}
