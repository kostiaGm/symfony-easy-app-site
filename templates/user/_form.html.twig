{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="row">
    <div class="col-md-8"><a href="{{ path('app_user_index') }}">back to list</a></div>

    <div class="col-md-2 offset-md-2">
        {{ include('user/_delete_form.html.twig') }}
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>
</div>

{{ form_end(form) }}
