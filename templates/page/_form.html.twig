{% set route =  page.id is null ? 'app_page_new' : 'app_page_edit' %}

  {{ form_start(form) }}
    {{ form_widget(form) }}
    {% include 'elements/action_buttons_form.html.twig' with {back_to_list_path: path('app_page_index')}%}
{{ form_end(form) }}
