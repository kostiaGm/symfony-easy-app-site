
<div class="card">
    <div class="card-header">
        {% include 'elements/form_tab.html.twig' with {items: [
            {title: 'Main', route:  page.id is null ? 'app_page_new' : 'app_page_edit', params: {id: page.id}},
            {title: 'Seo', route: 'app_seo', params: {id: page.id}, disable: page.id is null},
        ]}
        %}
    </div>
    <div class="card-body">
        <div id="menu-jstree">
            {{ form_start(form) }}

            {{ form_widget(form) }}
            <div class="row">
                <div class="col-md-8"><a href="{{ path('app_page_index') }}">back to list</a></div>

                <div class="col-md-2 offset-md-2 align-content-end">
                  {#  {{ include('page/_delete_form.html.twig') }}#}
                    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>




{{ form_end(form) }}
