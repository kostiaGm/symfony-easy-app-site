<form method="post" action="{{ path('app_gallery_setting_delete', {'id': gallery_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gallery_setting.id) }}">
    <button class="btn">Delete</button>
</form>
